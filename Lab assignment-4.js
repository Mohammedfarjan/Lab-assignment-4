// Define the quiz question and answer
const Quizquestions = [
    {
        question: ""
    }
];

// Initialize the score
let score = 0;

//Loop through each question
for (let i = 0; i < Quizquestions.length; i++) {
    // Prompt through each question
    const userAnswer = prompt(Quizquestions[i].question);
    
}